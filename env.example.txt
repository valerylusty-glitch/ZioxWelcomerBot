# Copie ce fichier en ".env" (à la racine, à côté de bot/ et api/) et remplis tes valeurs.
# bot/main.py ET api/server.py lisent tous les deux ce même fichier.

# Token obtenu via @BotFather sur Telegram
BOT_TOKEN=1234567890:AAExempleDeTokenAChanger

# URL HTTPS publique où est hébergée la mini-app (webapp/index.html)
WEBAPP_URL=https://ton-domaine.exemple/webapp/index.html

# Base de données partagée entre le bot et l'API (SQLite par défaut, suffisant pour démarrer)
DATABASE_URL=sqlite:///ziox.db

# Port sur lequel tourne l'API Flask
PORT=5000
